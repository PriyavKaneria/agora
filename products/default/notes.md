# Product Notes

Put your detailed product content in this folder.

The orchestrator ingests:
- `profile.toml` for structured attributes.
- `.md` and `.txt` files for long-form context.
- Image files (`.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`) as metadata paths.

Add as much context as needed to keep draft quality high.
